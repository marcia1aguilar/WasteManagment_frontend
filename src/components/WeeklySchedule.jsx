import "../styles/WeeklySchedule.css";

export default function WeeklySchedule({ schedule }) {
    return (
        <section className="weekly-schedule">
            <h2>Weekly Schedule</h2>
            <div className="schedule-grid">Schedule goes here</div>
        </section>
    );
}
